import { HideshowDirective } from './hideshow.directive';

describe('HideshowDirective', () => {
  it('should create an instance', () => {
    const directive = new HideshowDirective();
    expect(directive).toBeTruthy();
  });
});
